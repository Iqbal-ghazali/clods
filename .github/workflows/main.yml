name: test

on:
  push:
    branches:
    - main
    - release/*

jobs:
  my_job:
    name: deploy to staging
    runs-on: ubuntu-22.04

    
name: hello-world
'on': push
jobs:
  my-job:
      runs-on: ubuntu-latest
      steps:
        - name: my-step
          run: echo "Hello World!"
